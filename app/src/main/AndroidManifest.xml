<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cektrend.cekinhome">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cekinhome">
        <activity android:name=".features.history.HistoryActivity"></activity>
        <activity
            android:name=".features.SplashActivity"
            android:noHistory="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".features.pin.PinActivity"
            android:noHistory="true"></activity>
        <activity
            android:name=".features.main.MainActivity"
            android:configChanges="uiMode" />
    </application>

</manifest>